{/* CloseIcon.mdx */}

import { Meta, Story } from '@storybook/blocks';
import { Container } from '../Components';
import * as CloseIconStories from './CloseIcon.stories';

# renderCloseIcon

The prop renderCloseIcon allows you to swap out Vizzly components for your design system. It is likely you'll need to map the props between the two components.

<Meta of={CloseIconStories} />

### Demo

<Container>
  <Story of={CloseIconStories.Example} autoplay />
</Container>

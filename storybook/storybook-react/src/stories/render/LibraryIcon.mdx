{/* LibraryIcon`.mdx */}

import { Meta, Story } from '@storybook/blocks';
import { Container } from '../Components';
import * as LibraryIconStories from './LibraryIcon.stories';

# renderLibraryIcon

The prop renderLibraryIcon allows you to swap out Vizzly components for your design system. It is likely you'll need to map the props between the two components.

<Meta of={LibraryIconStories} />

### Demo

<Container>
  <Story of={LibraryIconStories.Example} autoplay />
</Container>

{/* ComponentOptionsMenuIcon`.mdx */}

import { Meta, Story } from '@storybook/blocks';
import { Container } from '../Components';
import * as ComponentOptionsMenuIconStories from './ComponentOptionsMenuIcon.stories';

# renderComponentOptionsMenuIcon

The prop renderComponentOptionsMenuIcon allows you to swap out Vizzly components for your design system. It is likely you'll need to map the props between the two components.

<Meta of={ComponentOptionsMenuIconStories} />

### Demo

<Container>
  <Story of={ComponentOptionsMenuIconStories.Example} autoplay />
</Container>

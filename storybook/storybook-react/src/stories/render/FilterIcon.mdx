{/* FilterIcon.mdx */}

import { Meta, Story } from '@storybook/blocks';
import { Container } from '../Components';
import * as FilterIconStories from './FilterIcon.stories';

# renderFilterIcon

The prop renderFilterIcon allows you to swap out Vizzly components for your design system. It is likely you'll need to map the props between the two components.

<Meta of={FilterIconStories} />

### Demo

<Container>
  <Story of={FilterIconStories.Example} autoplay />
</Container>

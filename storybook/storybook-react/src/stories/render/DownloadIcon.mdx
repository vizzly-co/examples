{/* DownloadIcon`.mdx */}

import { Meta, Story } from '@storybook/blocks';
import { Container } from '../Components';
import * as DownloadIconStories from './DownloadIcon.stories';

# renderDownloadIcon

The prop renderDownloadIcon allows you to swap out Vizzly components for your design system. It is likely you'll need to map the props between the two components.

<Meta of={DownloadIconStories} />

### Demo

<Container>
  <Story of={DownloadIconStories.Example} autoplay />
</Container>

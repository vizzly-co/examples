{/* DateTimeInput.mdx */}

import { Meta, Story } from '@storybook/blocks';
import { Container } from '../Components';
import * as DateTimeInputStories from './DateTimeInput.stories';

# renderDateTimeInput

The prop renderDateTimeInput allows you to swap out Vizzly components for your design system. It is likely you'll need to map the props between the two components.

<Meta of={DateTimeInputStories} />

### Demo

<Container>
  <Story of={DateTimeInputStories.Example} autoplay />
</Container>

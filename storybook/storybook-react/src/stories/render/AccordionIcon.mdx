{/* AccordionIcon`.mdx */}

import { Meta, Story } from '@storybook/blocks';
import { Container } from '../Components';
import * as AccordionIconStories from './AccordionIcon.stories';

# renderAccordionIcon

The prop renderAccordionIcon allows you to swap out Vizzly components for your design system. It is likely you'll need to map the props between the two components.

<Meta of={AccordionIconStories} />

### Demo

<Container>
  <Story of={AccordionIconStories.Example} autoplay />
</Container>

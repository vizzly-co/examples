{/* CanAddNewRows.mdx */}

import { Meta, Story } from '@storybook/blocks';
import { Container } from '../Components';
import * as CanAddNewRowsStories from './CanAddNewRows.stories';

# Allow Percentages Custom Field

## Feature Toggle

By default <code>canAddNewRows</code> is set to <code>true</code>. By setting it to false it stops your user from seeing the action and being able add a new Row.

<Meta of={CanAddNewRowsStories} />

<Container>
  <Story of={CanAddNewRowsStories.True} autoplay />
</Container>

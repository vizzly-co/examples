{/* ButtonDocs.mdx */}

import { Meta, Story } from '@storybook/blocks';
import { Container } from '../Components';
import * as AllowPercentagesCustomFieldStories from './AllowPercentagesCustomField.stories';

# Allow Percentages Custom Field

## Feature Toggle

By default <code>allowPercentagesCustomField</code> is set to <code>true</code>. It is represented in the UI as <strong>Custom Metrics</strong>

<Meta of={AllowPercentagesCustomFieldStories} />

<Container>
  <Story of={AllowPercentagesCustomFieldStories.True} autoplay />
</Container>
